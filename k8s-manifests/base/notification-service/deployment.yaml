apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: notification-service
          image: vignesh8386/smartpg-notification-service:v1.0.0
          ports:
            - containerPort: 4005
          envFrom:
            - configMapRef:
                name: smartpg-config
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: smartpg-secrets
                  key: MONGO_URI
                  optional: true
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: smartpg-secrets
                  key: JWT_SECRET
                  optional: true
            - name: PORT
              value: "4005"
          livenessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 4005
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
